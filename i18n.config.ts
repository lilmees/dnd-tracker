import en from './locales/en.json'
import nl from './locales/nl.json'

export default {
  missingWarn: false,
  legacy: false,
  locale: 'nl',
  fallbackLocale: 'nl',
  messages: {
    en,
    nl
  }
}
